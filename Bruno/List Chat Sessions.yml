info:
  name: List Chat Sessions
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8000/chat/sessions
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
