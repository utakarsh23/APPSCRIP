info:
  name: List Files
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8000/files
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
